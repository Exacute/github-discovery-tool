# Core dependencies
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
python-dotenv==1.0.0
requests==2.31.0
flask-cors==4.0.0

# Optional: Embeddings providers (choose one based on your setup)
# For OpenAI embeddings:
# openai==1.3.0

# For local embeddings (sentence-transformers):
# sentence-transformers==2.2.2

# For Ollama (no additional Python package needed, just set EMBEDDINGS_PROVIDER=ollama)
